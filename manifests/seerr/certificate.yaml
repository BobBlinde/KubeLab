apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: seerr-tls
  namespace: seerr
spec:
  secretName: seerr-tls
  dnsNames:
    - seerr.blinde.net
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
