apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-library
  namespace: immich
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  volumeName: immich-library
  resources:
    requests:
      storage: 1Ti
