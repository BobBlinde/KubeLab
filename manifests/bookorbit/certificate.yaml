apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bookorbit-tls
  namespace: bookorbit
spec:
  secretName: bookorbit-tls
  dnsNames:
    - bookorbit.blinde.net
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
