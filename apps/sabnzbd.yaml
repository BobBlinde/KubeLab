apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sabnzbd
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/BobBlinde/KubeLab.git
    targetRevision: main
    path: manifests/sabnzbd

  destination:
    server: https://kubernetes.default.svc
    namespace: sabnzbd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
